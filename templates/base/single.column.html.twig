{% extends 'base/container.html.twig' %}

{% block container %}
    <div class="row my-5">
        <div class="col-sm mx-auto">
            {% if title is defined %}<h3>{{ title }}</h3>{% endif %}
            {% block body %}{% endblock %}
        </div>
    </div>
{% endblock %}
